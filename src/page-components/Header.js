import React from 'react';
import {
  
} from './aurora';

class Header extends React.Component {
  render() {
    return(
      {/* You could have different themes for page components*/}
    );
  }
}

export default Header;